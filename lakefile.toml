# Lake configuration file
# This file is optional when lakefile.lean exists
# Keeping minimal configuration to avoid conflicts

name = "Thoughts"
version = "0.1.0"
defaultTargets = ["thoughts"]

# Use lakefile.lean for detailed configuration
